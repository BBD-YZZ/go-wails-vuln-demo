# 漏洞扫描工具

这是一个基于Go和Wails的通用漏洞扫描工具，具有炫酷的用户界面，支持代理设置和实时扫描日志。

## 功能特性

- **直观的用户界面**：采用现代化设计风格，界面炫酷易用
- **灵活的目标配置**：支持批量输入扫描目标
- **多种扫描模式**：快速扫描、完整扫描、自定义扫描
- **代理支持**：支持HTTP和SOCKS5代理，支持认证
- **实时日志**：实时显示扫描过程和结果
- **详细报告**：提供详细的漏洞信息和修复建议

## 系统要求

- Go 1.21+
- Node.js 15+
- npm

## 安装和运行

### 开发模式

1. 克隆或下载项目
2. 安装依赖：
   ```bash
   cd vuln-scanner
   go mod tidy
   cd frontend
   npm install
   ```

3. 启动开发服务器：
   ```bash
   cd ..
   wails dev
   ```

### 构建发行版

```bash
wails build
```

生成的可执行文件将在 `build/bin` 目录中。

## 使用说明

### 1. 配置扫描参数
- 在顶部输入框中输入目标地址（每行一个）
- 选择扫描类型（快速/完整/自定义）
- 设置并发线程数和超时时间
- 点击"代理设置"配置代理（可选）

### 2. 开始扫描
- 点击"开始扫描"按钮启动扫描
- 观察中间部分的扫描结果
- 查看底部的详细扫描日志

### 3. 查看结果
- 扫描完成后，结果会在中间区域显示
- 包含漏洞类型、严重程度、目标地址、漏洞详情等信息
- 提供具体的修复建议

## 技术架构

### 前端
- React + TypeScript
- 使用组件化架构
- 实时数据更新

### 后端
- Go语言编写
- 支持并发扫描
- 内置日志系统
- 代理支持

## 二次开发

本工具设计为通用框架，您可以：

1. 在 `app.go` 中的 `StartScan` 方法中添加您自己的漏洞检测逻辑
2. 修改扫描算法以适应特定漏洞类型
3. 扩展漏洞类型和检测规则
4. 自定义报告格式

## 文件结构

```
vuln-scanner/
├── app.go              # Go后端主要逻辑
├── main.go             # Wails应用入口
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── App.css
│   │   └── components/ # React组件
│   └── package.json
├── go.mod
├── go.sum
└── wails.json          # Wails配置文件
```

## 注意事项

- 仅用于授权的安全测试
- 请遵守相关法律法规
- 扫描过程中可能会产生大量网络请求，请谨慎使用

## 许可证

此项目仅供学习和教育目的使用。